package be.hogent.vic.database

import androidx.room.*

@Dao
interface VirtualMachineDatabaseDto {

//    @Query("SELECT * from custom_joke_table WHERE Id = :key")
//    suspend fun get(key: Int): DatabaseVirtualMachine?
}